# Managed by the Archenemy installer; do not edit in place.
# Placeholders:
#   __USER__         -> replaced with the invoking user name
#   __SUDOERS_FILE__ -> replaced with /etc/sudoers.d/archenemy-first-run (or custom path)

# During the first run we allow the invoking user passwordless sudo for
# all commands. The installer cleanup phase removes this file once the
# system has finished configuring itself.
__USER__ ALL=(ALL) NOPASSWD: ALL
